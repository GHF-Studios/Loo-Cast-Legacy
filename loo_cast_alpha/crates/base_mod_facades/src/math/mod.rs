//! # Scope
//! - Provides math types and functionality with extremely large precision and range via ergonomic facades.
//! - Allows interop with `normal` math types.
//! - Any functionality involving ONLY `normal` math types is intentionally NOT covered by this module.
//! - By default, only bevy and rhai facades are provided.
